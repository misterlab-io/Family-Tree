# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This repository is currently empty and awaiting its initial commit. Once project files are added, update this file with:

1. **Build, lint, and test commands** — e.g. `npm run dev`, `npm test`, `npm run lint`, how to run a single test.
2. **Architecture overview** — the high-level structure, how data flows between layers, key abstractions, and non-obvious design decisions that require reading multiple files to understand.
3. **Conventions** — naming patterns, file organization rules, or project-specific practices that differ from defaults.
